import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora2022pippo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
